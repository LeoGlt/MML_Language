library(utils)
library(dplyr)library(caret)library(caret)library(caret)library(caret)library(caret)library(caret)library(caret)library(caret)library(caret)library(caret)data <- read.csv('iris.csv', sep=',')
y <- data %>% select('variety')
X <- data %>% select(-'variety')
set <- sample(1:nrow(X),floor(0.8*nrow(X)))
X_train <- X[set,]
X_test <- X[-set,]y_train <- y[set]
y_test = y[-set]
mat_conf <- table(y_pred,y_test)
accuracy = precision(mat_conf, reference = y_test, relevant = "Relevant")
mat_conf <- table(y_pred,y_test)
recall = recall(mat_conf, reference = y_test, relevant = "Relevant")
mat_conf <- table(y_pred,y_test)
mat_conf <- table(y_pred,y_test)
F1 = F_meas(mat_conf, reference = y_test, relevant = "Relevant", beta = 1)
mat_conf <- table(y_pred,y_test)
 first_row = mat_conf[1,1] / (mat_conf[1,1] + mat_conf[1,2])  
 second_row <- mat_conf[2,2] / (mat_conf[2,1] + mat_conf[2,2])  
 balanced_accuracy = (first_row + second_row)/2mat_conf <- table(y_pred,y_test)
accuracy = precision(mat_conf, reference = y_test, relevant = "Relevant")
mat_conf <- table(y_pred,y_test)
recall = recall(mat_conf, reference = y_test, relevant = "Relevant")
mat_conf <- table(y_pred,y_test)
mat_conf <- table(y_pred,y_test)
F1 = F_meas(mat_conf, reference = y_test, relevant = "Relevant", beta = 1)
mat_conf <- table(y_pred,y_test)
 first_row = mat_conf[1,1] / (mat_conf[1,1] + mat_conf[1,2])  
 second_row <- mat_conf[2,2] / (mat_conf[2,1] + mat_conf[2,2])  
 balanced_accuracy = (first_row + second_row)/2